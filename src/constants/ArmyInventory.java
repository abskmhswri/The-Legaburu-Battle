package constants;

import java.util.ArrayList;
import java.util.List;

public class ArmyInventory {
    public static int strength[] = {100, 50, 10, 5};
    public static int power[] = {1,2,4,8}; //in terms on basic unit i.e. Horse (here). We are not using it now but this will be
                                           // useful in future when power of each right unit is not more by same factor as compared to left unit
    public static int powerFactor = 2;
}
